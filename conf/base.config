process {

    cpus   = { task.attempt > 1 ? task.previousTrace.cpus       * 2         : 2    }
    memory = { task.attempt > 1 ? task.previousTrace.memory     * 2         : 48.GB }

    errorStrategy = { task.exitStatus in ((130..145) + 104) ? 'retry' : 'finish' }
    maxRetries    = 3

}